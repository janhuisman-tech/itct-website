# itct-website
├── index.html          ← de grote site (artifact 1)
├── netlify.toml        ← artifact 3 (alleen de eerste 7 regels, zonder de commentaar)
└── admin/
    ├── index.html      ← artifact 2
    └── config.yml      ← artifact 1
